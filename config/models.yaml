# Codex model catalog
#
# Runtime models are sourced from Codex backend fetches and cached in
# data/models-cache.yaml after each successful refresh. This file intentionally
# carries no bundled model or alias fallback so the dashboard only reflects
# models available from the current fetched account/plan snapshots.

models: []
aliases: {}
